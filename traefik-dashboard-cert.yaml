apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls-traefik-dashboard
  namespace: traefik
spec:
  secretName: tls-traefik-dashboard
  dnsNames:
  - traefik.jeffriffle.com
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
