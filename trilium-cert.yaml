apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls-trilium
  namespace: trilium
spec:
  secretName: tls-trilium
  dnsNames:
  - trilium.jeffriffle.com
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
