apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cloned-trilium-data-pvc
  namespace: trilium
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
